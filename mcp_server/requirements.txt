# HR SoT MCP server runtime dependencies.
# Kept separate from the main app so the MCP image stays small and independent.
mcp>=1.28.0
httpx>=0.27.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
uvicorn[standard]>=0.32.0
